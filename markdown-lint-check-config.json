{
    "ignorePatterns": [
        {
            "pattern": "^http://localhost"
        }
    ],
    "timeout": "30s",
    "retryOn429": true,
    "retryCount": 3,
    "fallbackRetryDelay": "60s",
    "aliveStatusCodes": [0, 200, 202, 206]
}